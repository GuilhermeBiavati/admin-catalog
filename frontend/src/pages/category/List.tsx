import * as React from 'react';
import { Page } from '../../components/Page';
type Props = {};

const List = (props: Props) => {
  return <Page title={'Listar Categorias'}></Page>;
};
export default List;
